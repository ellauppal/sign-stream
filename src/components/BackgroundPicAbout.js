import classes from './BackgroundPicAbout.module.css';

function BackgroundPicAbout() {

    return (
        <div className={classes.backAbout}>
        </div>
      );
    }

export default BackgroundPicAbout;